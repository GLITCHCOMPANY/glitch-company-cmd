$GLITCH_VERSION="1.0.1"

Write-Host "=========================="
Write-Host "GLITCH COMPANY CMD"
Write-Host "VERSION $GLITCH_VERSION"
Write-Host "=========================="

doskey /ai=ollama run phi3:mini $*

doskey /download=winget install -e --id $*

doskey /update=powershell -Command "$code=(Invoke-WebRequest 'https://raw.githubusercontent.com/GLITCHCOMPANY/glitch-company-cmd/main/code.txt').Content; Invoke-Expression $code"

Write-Host "SYSTEM LOADED"
